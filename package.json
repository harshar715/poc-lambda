{
  "name": "lambda-sample-app",
  "version": "1.0.0",
  "description": "Sample Lambda application with GET endpoint and event-based function for POC",
  "main": "handler.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "lambda",
    "aws",
    "poc"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.490.0",
    "@aws-sdk/client-sns": "^3.490.0"
  }
}

